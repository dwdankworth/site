{
  "private": true,
  "name": "danksite",
  "description": "Interactive CLI-style portfolio website",
  "scripts": {
    "lint": "eslint js/ tests/",
    "test": "npx playwright test",
    "test:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.50.0",
    "eslint": "^10.0.2",
    "serve": "^14.2.0"
  }
}
